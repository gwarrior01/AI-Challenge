{
  "mcpServers": {
    "everything": {
      "description": "Демо-сервер MCP со всеми возможностями протокола (echo, add, …)",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-everything"]
    },
    "javadocs": {
      "description": "Документация Java-библиотек (удалённый сервер, Streamable HTTP)",
      "url": "https://www.javadocs.dev/mcp"
    },
    "github": {
      "description": "Пример выключенного сервера с токеном из окружения",
      "enabled": false,
      "url": "https://api.githubcopilot.com/mcp/",
      "headers": { "Authorization": "Bearer ${GITHUB_TOKEN}" }
    },
    "java-profiler": {
      "description": "Профилирование Java-приложений на этой машине (свой сервер, Streamable HTTP; запуск: cargo run -p java-profiler-mcp)",
      "url": "http://127.0.0.1:8091/mcp"
    },
    "scheduler": {
      "description": "Планировщик: отложенные и периодические задания с хранением в SQLite и сводками (свой сервер, Streamable HTTP; запуск: cargo run -p scheduler-mcp)",
      "url": "http://127.0.0.1:8092/mcp"
    }
  }
}
